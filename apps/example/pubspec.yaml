name: voo_navigation_demo
description: Demo + widget previews for the voo_navigation packages.
publish_to: 'none'
version: 1.0.0
resolution: workspace

environment:
  sdk: '>=3.8.1 <4.0.0'
  flutter: '>=3.29.0'

dependencies:
  flutter:
    sdk: flutter
  voo_navigation:
  voo_navigation_core:
  voo_navigation_bar:
  voo_navigation_rail:
  voo_navigation_drawer:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
